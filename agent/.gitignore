node_modules/
dist/
.env
!.env.example
test-fixtures/
.langgraph_api/

# Root .gitignore has `tools/` (unanchored) which also hides agent/src/tools — re-include it
!src/tools/

# SQLite riêng của agent (drafts, price observations)
*.db
*.db-shm
*.db-wal
data/
